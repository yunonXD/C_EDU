#include<stdio.h>

//바이너리 데이터
// size_t fread(void *buffer, size_t size,
//size_t count, FILE *stream)

//전달인자 count 만큼 읽어라. 즉 6이면 꼭 6개 읽어야함.
//성공하면 count return
//실패 or 파일의 끝이면 count 보다 작은 값 return

int main(int argc, char *argv[]){






    return 0;
}